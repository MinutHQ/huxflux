/**
 * Threshold above which the stacked diff view falls back to a flat "open file"
 * list, because rendering hundreds of inline diffs is prohibitively expensive.
 */
export const STACKED_DIFF_THRESHOLD = 100
