// Public re-export of the title-generation service. Implementation lives in
// `service/title.ts` (private subfolder).

export { generateTitle, deriveTitle, titleToBranchSlug } from "./service/title.js"
