// Re-exported from @hive/shared — kept as a shim so existing imports work unchanged.
export { useAgentEvents, connectBackgroundServer } from "@hive/shared"
export type { ServerEvent } from "@hive/shared"
